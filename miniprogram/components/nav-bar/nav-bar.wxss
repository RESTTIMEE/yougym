.nav-bar { display: flex; align-items: center; justify-content: center; height: 88rpx; padding-top: 44rpx; }
.nav-title { color: var(--text-white); font-size: 34rpx; font-weight: bold; }
.back-btn { position: absolute; left: var(--spacing-md); color: var(--text-white); font-size: 32rpx; }
